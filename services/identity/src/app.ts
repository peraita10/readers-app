import express,{type Express,type NextFunction,type Request,type Response}from"express";import{randomUUID}from"node:crypto";import type{HealthResponse}from"@readers/contracts";import{AppError}from"@readers/errors";import{createLogger}from"@readers/logger";const service="identity" as const;const logger=createLogger(service);export const app:Express=express();app.disable("x-powered-by");app.use(express.json());app.use((req:Request,res:Response,next:NextFunction)=>{const requestId=req.header("x-request-id")??randomUUID();res.setHeader("x-request-id",requestId);const startedAt=Date.now();res.on("finish",()=>logger.info({requestId,method:req.method,path:req.path,statusCode:res.statusCode,durationMs:Date.now()-startedAt},"request completed"));next();});app.get("/health",(_req:Request,res:Response<HealthResponse>)=>res.json({service,status:"ok",timestamp:new Date().toISOString()}));app.use((_req:Request,_res:Response,next:NextFunction)=>next(new AppError(404,"ROUTE_NOT_FOUND","Route not found")));app.use((error:unknown,req:Request,res:Response,_next:NextFunction)=>{const requestId=res.getHeader("x-request-id");if(error instanceof AppError)return res.status(error.statusCode).json({error:{code:error.code,message:error.message,requestId}});logger.error({err:error,requestId,method:req.method,path:req.path},"unhandled error");return res.status(500).json({error:{code:"INTERNAL_SERVER_ERROR",message:"Internal server error",requestId}});});